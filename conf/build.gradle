apply plugin: 'java-library'

ext.moduleName = 'data.conf'

dependencies {
    implementation(
            project(':usecase'),
            project(':domain'),
            project(':controller'),
            project(':gateways'),
            project(':repository')

    )
}